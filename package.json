{
  "name": "car-transfer-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot for calculating Spanish car transfer costs using Kapso and Convex",
  "main": "index.js",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "setup": "convex dev --once",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.17.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@cloudflare/workers-types": "^4.20250129.0"
  }
}
